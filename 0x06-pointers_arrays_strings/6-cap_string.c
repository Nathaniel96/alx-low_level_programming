#include "main.h"

/**
 * cap_string - Prepare for the worst. Capitalize on what comes
 * @n: pointer
 * Return: 0
 */

char *cap_string(char *n)
{
	int index = 0;

	while (str[index])
	{
		while (
	}
}
